\chapter{Implementación del algoritmo}
\hg{Está muy bien, lo único que me gustaría tener referencias al algoritmo que entiendo vas a poner en la sección 3 para no perderme tanto cuando leo}
\section{Introducción}
La herramienta MTSA \cite{mtsa} es el framework de desarrollo realizado en Java para varias iniciativas de LaFHIS en el contexto de la investigación sobre modelado y verificación de sistemas concurrentes, así como síntesis de controladores. Es por eso que tanto el algoritmo composicional presentado por Uchitel y Gagliardi \todo{agregar cita} como la versión de WSOE utilizada para la composicionalidad fueron implementados en esta herramienta. Era entonces natural realizar el desarrollo del algoritmo de Branching Equivalence sobre el mismo framework. A su vez, nos permitirá comparar de forma más verídica ambos algoritmos, realizando pruebas de Benchmarks tanto para tiempos de ejecución como para el uso de memoria.
En primer lugar, se realizó la implementación del algoritmo de Branching Equivalence en el archivo BranchingEquivalence.java, creado en mtstools/DCS/Compositional/ ya que bajo este módulo están las herramientas utilizadas por el algoritmo composicional.

A su vez, en CompositionalApproach.java se reemplazó la llamada original al algoritmo de WSOE por la llamada a la nueva función que utiliza el algoritmo de Groote y Jansen \hg{cita}, reemplazando también los preprocesamientos necesarios para los algoritmos que se hacen en algunos casos fuera de las funciones principales.

La llamada al algoritmo de Branching Equivalence queda entonces de esta forma
\begin{lstlisting}[language=Java, numbers=none]                                
  Pair<Pi_s, Pi_t> partition
                    = BranchingEquivalence.getPartitions(lts, alphabet, controllables, fluentGoals);

  MTS<Long, String> minimizationResult = BranchingEquivalence.buildMinimisedMTSFromPartition(lts, tauLabels, controllables, partition);
\end{lstlisting}        

donde en la función getPartitions(...) se implementa el algoritmo de \textit{Branching Equivalence } de Groote y Jansen \hg{cita} y se devuelve la partición final de estados y transiciones, y en buildMinimisedMTSFromPartition(...) se construye el MTS minimizado a partir de la partición calculada.

A su vez, el algoritmo recibe como parámetros el LTS en realidad a minimizar, el alfabeto local, el traductor total y los fluents objetivo. \hg{hay un par de estas cosas que habría que chequear si explicar antes que son o sino tenes que explicarlas acá, por ej. traductor total, fluents objetivo, etc.} 

En primer lugar, la modalidad de desarrollo fue implementar la lógica del algoritmo lo más fielmente al paper posible, es decir, sin tomar en cuenta las optimizaciones ni las cotas de complejidad. En la versión cero ($v_0$), el algoritmo replica la estructura del paper pero reemplazando las estructuras de datos propuestas por estructuras nativas de MTSA.

\section{Primera implementación del algoritmo}

Como fue explicado en el capítulo 3, el algoritmo de Groote y Jansen \hg{cita} mantiene dos estructuras de refinamiento: una partición de estados $\Pi_s$ y una partición de transiciones $\Pi_t$. En $\Pi_s$ se encuentran los \textit{bloques} de estados estables tal que si dos estados están en distintos bloques entonces \textbf{no} son \textit{Branching Equivalent}. En $\Pi_t$ se encuentran los \textit{bunches} de transiciones no-inertes, es decir, las transiciones que pueden distinguir estados. Ambas estructuras se refinan a la par: cada vez que un bloque de estados se parte en dos, se actualizan los bunches para reflejar el nuevo bloque, y cada vez que un bunch se parte en slices por (acción, bloque destino), se generan nuevos splitters para estabilizar los bloques afectados. El proceso continúa hasta llegar al punto fijo donde no quedan ni splitters pendientes ni bunches por refinar: las clases de equivalencia son entonces los bloques finales de $\Pi_s$.

La idea para esta primera implementación fue mantener la estructura del paper, donde el loop principal itera sobre cada conjunto de transiciones no triviales que están en $\Pi_t$, tomando un subconjunto chico de transiciones. Este subconjunto de transiciones, es separado de su bunch original y añadido a $\Pi_t$ como un nuevo bunch. Luego, se deben refinar los bloques de $\Pi_s$ para que queden estables respecto al nuevo conjunto de transiciones obtenido.

Una decisión importante para esta primera versión fue la elección de las estructuras de datos a utilizar, ya que inciden en la complejidad final de la implementación. En esta primera versión, buscando en primer lugar comprender la lógica en sí del algoritmo y poder verificar su correctitud, se eligieron estructuras de datos simples y nativas, lo más similar posible a las del paper. 

$\Pi_s$ se implementó como \texttt{List$<$Set$<$Long$>$$>$}, es decir una lista de conjuntos de estados. Cada \texttt{Set$<$Long$>$} representa un bloque de estados.
$\Pi_t$ se implementó como \texttt{List$<$Set$<$Triple$<$Long, String, Long$>$$>$$>$}, es decir una lista de conjuntos de transiciones. Cada \texttt{Set$<$Triple$<$Long, String, Long$>$$>$} representa un bunch de transiciones no-inertes. \hg{Está muy bien todo esto, creo que quizá si modificas arriba los tipos de datos para hacerlos más pseudocódigo podes hacerlo acá también}

A su vez, se debió utilizar una estructura \texttt{Pi\_t\_cola} para poder iterar sobre los bunches a la vez que se actualizaba la lista de pendientes a refinar.
Tal como se menciona en el paper de Groote y Jansen, se utiliza también la estructura del \texttt{splitterList} para almacenar los splitters generados durante el proceso de refinamiento. A su vez, se creó la clase \texttt{Splitter} para representar cada splitter, que contiene el bloque que se va a partir, el conjunto de transiciones que cruzan bloques y un flag de primary/secondary, un conjunto de transiciones marcadas para identificar que esos bloques son estables y un groupId para identificar qué Splitters se originaron a partir de la misma división inicial.

En primer lugar, el primer preprocesamiento requerido para este algoritmo es eliminar las componentes tau fuertemente conexas. La implementación de este paso usa la lógica del algoritmo de Kosaraju, que tiene una complejidad $O(n + m)$, por lo que cumple con la complejidad prometida por Groote y Jansen.
Luego, se debe inicializar $\Pi_s$, que arranca con dos bloques ($B_{vis}$ y $B_{invis}$), donde $B_{vis}$ contiene el conjunto de estados desde los cuales se puede alcanzar una transición visible y $B_{invis}$ es su complemento.
Para realizar esta inicialización, se ejecuta la función \texttt{computeBvis} que genera un grafo de predecesores, almacenando para cada transición, cuál fue su origen. Luego, identifica cuáles son visibles y realiza un DFS para propagar las acciones visibles e identificar desde qué estados son alcanzables. 
Por lo tanto, la complejidad resultante de la función es también $O(n + m)$. Finalmente, $B_{vis}$ se inicializa con el conjunto de estados identificados por esta función como visibles y $B_{invis}$ se inicializa con el resto.
$\Pi_t$ es inicializado con un único bunch que contiene todas las transiciones no-inertes.

Una de las primeras estructuras que también se deben inicializar en el preprocesamiento es el \texttt{stateToBlockMap}, definido como un mapa \texttt{Map$<$Long, Set$<$Long$>$$>$}, que almacena para cada estado a qué bloque de $\Pi_s$ pertenece. Esta estructura es indispensable para poder realizar el seguimiento y las modificaciones rápidas que identifican a cada estado con su bloque, ya que la lógica principal del algoritmo se basa en modificar estos bloques continuamente. A su vez, esta estructura es acompañada por la función \texttt{updateStateToBlockMap(Map$<$Long, Set$<$Long$>$$>$ map, Set$<$Long$>$ newBlock1, Set$<$Long$>$ newBlock2)} que recibe dos bloques nuevos y actualiza el mapa para que cada estado de cada bloque nuevo quede asociado a su bloque correspondiente. 

Luego del preprocesamiento y la inicialización de las estructuras de datos, el código tiene un loop principal que itera sobre los bunches. En cada ciclo, se toma un bunch $T$. Si $T$ es un bunch trivial, es decir que todas sus transiciones con la misma acción $a$ van a un mismo bloque, entonces el bunch $T$ no debe ser refinado. Si $T$ no es trivial, entonces se buscan todos los ''slices'' de $T$, es decir, se buscan y se separan los subconjuntos de $T$ que tienen la misma acción $a$ y bloque de destino $B'$. Luego, se itera sobre todos los slices de $T$ y se separa el primer slice encontrado que cumpla que es ''chico'', se lo identifica almacenándolo en la estructura \texttt{chosenTransitions}. Ahora, el objetivo es dividir a $T$, separando las \texttt{chosenTransitions} de $T$ y dejando el resto de $T$ en otra estructura que llamada \texttt{newBunch}. Luego, se agrega tanto el \texttt{chosenTransitions} como el \texttt{newBunch} a $\Pi_t$ y a la cola de bunches por refinar. Ahora, se tienen dos bunches en $\Pi_t$ que no son estables respecto a ambos bunches, por lo que se deben refinar los bloques de $\Pi_s$ para que lo sean. Para esto, se utiliza la función \texttt{findSplittableBlocks}, donde se buscan todos los bloques que contienen transiciones en los dos nuevos bunches. Por cada bloque $B$ encontrado, se crea un splitter primario y otro secundario y se guardan en una lista \texttt{splitterList}. El splitter primario contiene todas las transiciones de \texttt{chosenTransitions} que salen de $B$, y el splitter secundario contiene todas las transiciones de \texttt{newBunch} que salen de $B$. A su vez, se debe marcar como transiciones necesarias a reestabilizar todas las transiciones de \texttt{chosenTransitions}, es decir, las marcas del splitter primario. En esta primera versión, se utiliza un mapa \texttt{Map$<$Splitter, Set$<$Triple$<$Long, \allowbreak String, Long$>$$>$$>$ markings} para almacenar estas marcas, asociando a cada splitter el conjunto de transiciones que fueron marcadas para la reestabilización. Por cada estado que es origen de una transición primaria, se debe identificar si ese estado también tiene una transición secundaria, y marcar a una transición secundaria por cada estado, añadiéndola a \texttt{markings}, asociadas al splitter secundario. 

Luego, se itera sobre la \texttt{splitterList}. Por cada bloque $B$ que se está observando para realizar la partición, se define el conjunto de estados $R$ que son los estados de $B$ que pueden alcanzar inertemente una transición del splitter y el conjunto de estados $U$ que son los que no. Esta división en nuevos bloques $R$ y $U$ se hace a través de una función "split" que se detallará más adelante. Siguiendo la lógica del paper, si el splitter actual era primario, entonces luego del split, el bloque $U$ es estable respecto al bunch \texttt{chosenTransitions}, por lo que se debe crear un nuevo splitter secundario para el bloque $B$, que solo contiene las transiciones de \texttt{chosenTransitions} que salen de $R$. Es decir, se reemplaza el splitter secundario de $B$ por el nuevo splitter secundario para $R$. Al realizar el split de $R$ y $U$, las transiciones inertes salientes de $R$ hacia $U$, antes eran inertes pues estaban dentro del bloque $U$, pero ahora es necesario identificarlas como no-inertes, ya que ahora cruzan dos bloques separados. Para realizar esta identificación, se utiliza la función \texttt{findNewNonInertTransitions}. Estas nuevas transiciones no-inertes, conforman un nuevo bunch que se debe agregar a $\Pi_t$. A su vez, tal como se menciona en el paper, $R$ puede haberse vuelto inestable respecto al nuevo bunch creado. Por ello, se vuelve a utilizar la función split para volver a dividir $R$ en dos bloques, $N$ y $R'$. Si $N$ y $R'$ no son vacíos, entonces se reemplaza a $R$ de $\Pi_s$ por $N$ y $R'$. 

Acorde con el análisis hecho en el paper, la estabilidad no se preserva cuando aparecen nuevos estados bottom, por lo que se debe estabilizar al nuevo bloque $N$ con respecto a todos los bunches a donde $N$ tenga transiciones salientes. Para ello, se identifican todos los bottom states de $N$ y se itera sobre $\Pi_t$ para encontrar todos los bunches que contienen alguna transición saliente de $N$. Por cada bunch encontrado, se crea un splitter secundario. Por último, se itera sobre los estados bottom identificados de $N$ y se marca a una transición por cada estado. Estas marcas se realizan agregando a la primera transición del estado en una lista de estados \texttt{marksForRestabilization}.

Retomando, la función split es el corazón del algoritmo. La función split es la que se encarga de dividir al bloque $B$ en los bloques $R$ y $U$, y la implementación de esta función es la que hace la diferencia en el paper para poder alcanzar la cota de la complejidad. En el paper se detalla a la función split como dos corutinas que se ejecutan en paralelo. Sin embargo, con vistas a nuestro objetivo de poder realizar primero una primera implementación del algoritmo y validar su correctitud, en esta primera versión no se implementó la función como dos corutinas. La función split tiene la siguiente firma: 

\begin{lstlisting}[language=Java, numbers=none]                                
      private static Pair<Set<Long>, Set<Long>> split(
            Set<Long> B,
            Set<Triple<Long, String, Long>> transitions,
            Set<Triple<Long, String, Long>> currentMarks,
            MTS<Long, String> toMinimise,
            Set<String> tauLabels,
            Map<Long, Set<Long>> stateToSCCMap)
\end{lstlisting}   

Es decir, la función split recibe como argumentos el bloque $B$ a dividir, las transiciones del splitter asociado, las marcas del splitter asociado, el LTS a minimizar, el conjunto de etiquetas identificadas como tau y el mapa de estados a bloques. A su vez, \texttt{split} devuelve un par de conjuntos de estados, el bloque $R$ y el bloque $U$. En esta versión, \texttt{split} se limita a calcular el conjunto $R$ usando backward reachability para las transiciones inertes. Para comenzar este procesamiento, todos los estados con alguna transición marcada (es decir, incluida en \texttt{currentMarks}), son puestos en el nuevo bloque $R$. A su vez, se recorren todas las transiciones del splitter para incluir también en $R$ a los estados desde los cuales estas transiciones salen. Luego, se construye el mapa \texttt{inertPredecessorsInB}, que almacena para cada estado cuáles son sus predecesores inertes. Luego, se propagan las transiciones inertes hacia atrás, utilizando una estructura \texttt{worklistR} que comienza con todos los estados de $R$, y por cada uno de ellos busca qué estados son predecesores inertes y los agrega a $R$ y a la worklist para procesarlos también. El proceso continúa hasta que no queden predecesores inertes y por lo tanto, $R$ sea estable. Luego, se devuelve el nuevo bloque $R$ y el nuevo bloque $U$, definido como el complemento de $R$ en $B$.

\section{Primeros tests}
Como se mencionó anteriormente, la estrategia fue desarrollar una primera versión fiel al algoritmo para entender la lógica y poder validar su correctitud. Por eso, luego de realizar esta primera implementación, se desarrollaron casos de tests para verificar los funcionamientos básicos del algoritmo. Se realizaron en primera instancia tests unitarios básicos, con casos desde 2 estados hasta 80 estados. Se creó un archivo de testeo unitario llamado BranchingEquivalenceTest.java, ubicado en mtstools/DCS/Compositional/Tests/, donde se implementó la lógica básica para a raíz de archivos .lts llamar al algoritmo de Branching Equivalence y luego del algoritmo de Weak Equivalence y comparar el grafo resultante obtenido. 
Para esto, primero se realizaron pruebas de todos los casos donde ambos algoritmos tenían que reducir al mismo gráfico, es decir, casos donde los algoritmos se comportaran de forma equivalente. Luego de validar estos casos, el objetivo fue encontrar un caso donde la lógica de los algoritmos difiera, es decir que por el caso de estudio del grafo elegido, ambas reducciones por la definición de equivalencia que utilizan, se comporten de forma diferente \hg{como?, quizá acá o en algún lado conviene mostrar este ejemplo donde un algoritmo debería reducir y el otro no}. Para ello, se implementó el caso de test presentado por \todo{agregar cita} ROB J. VAN GLABBEEK y W. PETER WEIJLAND en https://dl.acm.org/doi/epdf/10.1145/233551.233556 . Este caso de test fue importante para poder asegurar que el algoritmo de Branching Equivalence realmente funcionara de la forma esperada, permitiendo validar de forma más robusta su implementación. A su vez, permitió desarrollar a un nivel más práctico las diferencias reales entre ambos algoritmos.

\subsection{Integración con el algoritmo composicional}
Luego de realizar los primeros tests, se revisó también la integración del nuevo algoritmo de branching equivalence con MTSA. En este contexto, se pudó detectar que en el contexto del algoritmo, una acción fluent se debería distinguir de las transiciones tau, y aun cuando esté identificada como local, no debe ser tratada como tau. Es por esto, que en la siguiente versión se agrega una validación para eliminar a las acciones fluents del conjunto de \texttt{tauLabels}.

Otra diferencia a como estaba implementado el algoritmo es que en MTSA el estado de error $-1L$ es particular. En el algoritmo inicial, esto no estaba contemplado, pero luego al realizar las primeras pruebas, lo modifiqué para que el estado de error tenga un bloque inicial propio.

A su vez, para poder integrar el algoritmo de minimización de branching en el algoritmo composicional, se creó la función \texttt{buildMinimisedMTSFromPartition}, que a partir de una partición que devuelve el algoritmo de branching bisimilarity se crea el MTS correspondiente. 

\section{Primeras optimizaciones}

\subsection{Cambio de estrategia: dos fases que alternan}
Para la primera versión del algoritmo con optimizaciones, la mayor diferencia con la estructura del paper es que se implementan dos fases que alternan al mismo nivel en lugar de anidar un while externo sobre bunches con un for interno sobre splitters. 
Si bien este cambio no afecta la complejidad asintótica, mejora el entendimiento del algoritmo y ayuda a introducir los cambios que mejoran el uso de las estructuras.
A su vez, también permite tener dos fases separadas y poder utilizarlas como elemento de medición para contar iteraciones y tiempos. Entonces, se modifica el algoritmo para que esté compuesto por dos fases: la primera donde se realiza la estabilización de estados y la segunda donde se refinan los bunches correspondientes. 

En la primera fase, se itera sobre la splitter list, que almacena una estructura por cada split pendiente, indicando que falta estabilizar el bloque B con el conjunto de transiciones asociadas y las marcas correspondientes. Por cada splitter, si el bloque no está en $\Pi_s$ quiere decir que ya fue partido por un splitter anterior y se descarta. Esto no pasará en la primera ronda pero puede suceder en las rondas futuras. Si no, se ejecuta el \texttt{split(B, transitions, marks)} que devuelve los bloques R y U al igual que en la primera versión. Al dividir a B en R y U, hay que actualizar los splitters relacionados a B. En la primera versión, esto era necesario solo para el splitter secundario, pero como ahora el algoritmo se divide en dos fases, entonces se define la función \texttt{refineSplitters} que recorre la splitterList y por cada splitter relacionado al bloque B, se crean dos splitters nuevos para R y U recalculando nuevamente las marcas necesarias y manteniendo el groupId. Estos splitters son añadidos a la splitter list para que sean procesados a continuación. Al igual que antes, el split no solo afecta a los splitters, sino que también puede hacer que un bunch trivial se vuelva no trivial luego de la partición. Para encolar a estos bunches, en esta nueva versión se implementa la estructura nueva \texttt{targetStateToBunches} que es de tipo \texttt{Map\textless Long, Set\textless Set\textless Triple\textgreater\textgreater\textgreater} y permite almacenar por cada bloque B cuáles son los bunches que tienen alguna transición entrante a B. Esto permite no iterar sobre todo $\Pi_t$ para encontrar los bunches afectados y encolarlos fácilmente. Por último, también puede ser que haya transiciones inertes en el bloque b que al dividirlo ya no sean inertes. Este proceso se optimizó en esta versión para acercarse más a lo sugerido en el paper. Para ello, se utiliza una nueva estructura \texttt{newFrontiers} que es un \texttt{Queue\textless Pair\textless Set\textless Long\textgreater, Set\textless Long\textgreater\textgreater\textgreater}, donde cada elemento es un par que contiene un bloque en cada posición, por ejemplo \texttt{\textless R,U\textgreater} y \texttt{\textless U,R\textgreater}, indicando que se debe revisar si hay transiciones que vayan de R a U que anteriormente eran inertes pero ahora dejaron de serlo. Por cada par, se buscan estas transiciones y se registra como nuevo bunch en las estructuras de datos indicadas, como $\Pi_t$. Luego, al igual que en la versión inicial, se vuelve a partir el bloque R en N y R', pero utilizando los nuevos mecanismos detallados para esta versión. El invariante que se asegura al finalizar la Fase 1, es que $\Pi_s$ es estable y la \texttt{splitterList} finaliza vacía. 

La segunda fase toma un bunch no trivial T de $\Pi_t$. Para T, se inicializa un mapa \texttt{Map\textless Pair\textless String, Integer\textgreater, Set\textless Triple\textless Long, String, Long\textgreater\textgreater\textgreater} llamado \texttt{slices}, donde por cada par \texttt{\textless a, blockID\textgreater} se almacena el conjunto de transiciones de T que tienen acción a y entran al bloque blockID. Esta es otra optimización que se agrega en esta versión, ya que antes los bloques se representaban como un \texttt{Set\textless Long\textgreater} pero esto representaba un costo O(n) cada vez que se buscaba un bloque. En esta nueva versión, se utiliza la estructura \texttt{blockIdMap} implementada como un \texttt{IdentityHashMap\textless Set\textless Long\textgreater, Integer\textgreater}. Esta estructura asocia por cada bloque un Integer, que actúa como su blockID asociado, permitiendo obtener el número de bloque en O(1). Retomando el algoritmo, T es trivial cuando hay una sola slice, por lo que en ese caso si el bunch elegido tiene una sola slice, se toma otro bunch de $\Pi_t$ hasta obtener uno que no sea trivial. Al encontrar un bunch T no trivial, es decir que tiene más de un slice, al igual que en la primera versión, se eligen las \texttt{chosenTransitions} y se realiza el mismo procedimiento que el mencionado anteriormente para partir T en \texttt{chosenTransitions} y \texttt{newBunch}, también actualizando $\Pi_t$ y los splitter asociados.

Entonces, la estrategia en esta nueva versión del algoritmo fue cambiar el orden de la lógica para primero procesar todos los splitters pendientes en la fase 1, luego realizar la partición de un bunch no trivial en la fase 2 y encolar nuevos splitters, para finalmente volver a iterar los splitters en la fase 2. Estas dos fases se ejecutan de manera alterna hasta que las estructuras $\Pi_t$ y la \texttt{splitterList} se encuentran vacías, ya que eso asegura que todos los bloques son estables respecto a los conjuntos de transiciones, y que a su vez todas las transiciones son no triviales. 

\subsection{Implementación de nuevas estructuras}
Con el objetivo de optimizar los tiempos del algoritmo, se reemplazaron varias estructuras poco ágiles de la primera versión. Por ejemplo, $\Pi_t$, antes implementada como un \texttt{List\textless Set\textless Triple\textgreater\textgreater}, ahora fue implementada como un \texttt{Set\textless Set\textless Triple\textgreater\textgreater} con \texttt{IdentityHashMap}. A su vez, en la clase \texttt{Splitter} también se realizaron varias modificaciones. Previamente, las marcas se marcaban en un mapa asociando el splitter con las marcas, ahora en cambio se implementa un campo \texttt{marks} dentro de la propia clase. Por otro lado, en la primera versión del algoritmo se explicó que para detectar cuando un splitter primario tenía un splitter secundario, se buscaba directamente el siguiente en la fila de splitters, asumiendo una adyacencia. Eso era válido en esa versión, pero ahora al tener dos fases distintas, se necesitó implementar dentro de la clase \texttt{Splitter} el campo \texttt{groupId}, marcando que si dos splitters tienen el mismo groupId están emparejados.

\section{Alcanzando la complejidad teorica}

Luego de realizar las primeras optimizaciones mencionadas y validar que el algoritmo funcionara correctamente, solo quedaba implementar dos cambios claves para alcanzar la complejidad teorica de o(n log m) tal como se detalla en el paper. En particular, el paper menciona dos estructuras claves en la seccion "detalles de implementacion". Por un lado, detalla la implementacion de estados agrupados por bloques con la utilizacion de una estructura llamada "refinable partition". Por otro lado, se menciona la implementacion de una estructura para almacenar los bunches de transiciones llamada "Linked Refinable Partitions" a la que en el codigo final para esta tesis se decidio nombrar como "LinkedTransitionPartitions". Por ultimo, en esta nueva version se implementan tambien los cambios necesarios para poder ejecutar la funcion \texttt{split} como las dos corrutinas mencionadas en el paper.

\subsection{Refinable Partitions}

\begin{figure}[htbp]
    \centering
    
    % ==========================================
    % GRÁFICO (a) - LTS y Particiones (Versión Mejorada)
    % ==========================================
    \begin{subfigure}[b]{0.48\textwidth}
        \centering
\begin{tikzpicture}[>=Stealth, state/.style={draw, circle, inner sep=1pt, minimum size=0.55cm}, scale=0.9, transform shape]

        % ==========================================
        % 1. POSICIONAMIENTO DE NODOS (GRILLA EXACTA)
        % ==========================================
        \node[state] (s6) at (3.0, 6.6) {$s_6$};
        
        \node[state] (s5) at (0.5, 4.2) {$s_5$};
        \node[state] (s4) at (2.3, 4.2) {$s_4$};
        \node[state] (s2) at (3.7, 4.2) {$s_2$};
        
        \node[state] (s3) at (0.5, 2.4) {$s_3$};
        \node[state] (s1) at (2.8, 2.4) {$s_1$};
        \node[state] (s0) at (4.5, 2.4) {$s_0$};
        
        \node[state] (s7) at (1.5, 0.0) {$s_7$};
        \node[state] (s8) at (4.0, 0.0) {$s_8$};

        % ==========================================
        % 2. FORMAS DE FONDO Y CAJAS (SEPARADAS)
        % ==========================================
        \begin{scope}[on background layer]
            
            % --- Fila Superior ---
            % B1
            \draw[thick, fill=black!10, rounded corners=12pt] (-0.2, 5.9) rectangle (5.0, 7.3);
            \node at (0.2, 6.6) {$B_1$};
            
            % T'
            \draw (-0.2, 5.1) rectangle (5.0, 5.6);
            \node at (0.2, 5.35) {$T'$};
            \node at (1.3, 5.35) {$d$};
            \node at (3.5, 5.35) {$d$};
            
            % --- Fila Central ---
            % B4
            \draw[thick, fill=black!10, rounded corners=12pt] (-0.4, 1.6) rectangle (1.2, 4.8);
            \node at (0.9, 3.3) {$B_4$};
            
            % T_\tau (Caja vertical)
            \draw (1.4, 1.6) rectangle (1.8, 4.8);
            \node at (1.6, 4.5) {$T_\tau$};
            
            % B0 (Polígono suavizado exacto)
            \draw[thick, fill=black!10, rounded corners=12pt] 
                (1.9, 4.8) -- (4.4, 4.8) -- (4.4, 3.5) -- (3.4, 2.8) -- (3.5, 1.6) -- (1.9, 1.6) -- cycle;
            \node at (3.5, 4.6) {$B_0$};
            
            % B3
            \draw[thick, fill=black!10, rounded corners=12pt] (3.8, 1.6) rectangle (5.2, 3.2);
            \node at (4.2, 2.9) {$B_3$};
            
            % --- Fila Inferior ---
            % T''
            \draw (-0.2, 0.7) rectangle (2.2, 1.2);
            \node at (0.1, 0.95) {$T''$};
            \node at (0.8, 0.95) {$a$};
            \node at (2.0, 0.95) {$a$};
            
            % T
            \draw (2.4, 0.7) rectangle (5.2, 1.2);
            \node at (2.7, 0.95) {$T$};
            \node at (3.6, 0.95) {$b$};
            \node at (4.0, 0.95) {$b$};
            \node at (4.3, 0.95) {$c$};

            % B2
            \draw[thick, fill=black!10, rounded corners=12pt] (-0.2, -0.8) rectangle (5.2, 0.5);
            \node at (0.2, -0.1) {$B_2$};
            
        \end{scope}

        % ==========================================
        % 3. TRANSICIONES (FLECHAS)
        % ==========================================
        % Conexiones izquierdas
        \draw[->] (s5) to[bend right=30] node[left] {$a$} (s3);
        \draw[->] (s3) -- (s5);
        \draw[->] (s4) -- (s3);
        
        % Conexiones centrales y cruzadas
        \draw[->] (s4) -- (s2);
        \draw[->] (s2) -- (s3);
        \draw[->] (s2) -- (s1);
        
        % Conexiones hacia abajo (pasando por T'' y T)
        \draw[->] (s3) to[out=270, in=110] (s7);
        \draw[->] (s1) -- (s7);
        \draw[->] (s1) -- (s8);
        \draw[->] (s0) -- (s8);
        
        % Autoloop y conexión de s8 a s7
        \draw[->] (s8) edge [loop above, min distance=1.2cm, in=60, out=120] (s8);
        \draw[->] (s8) to[bend left=20] node[below] {$b$} (s7);
        
        % Conexiones hacia arriba (pasando por T')
        \draw[->] (s3) to[out=100, in=180] (s6);
        \draw[->] (s2) -- (s6);
        \draw[->] (s0) to[out=90, in=340] node[right, pos=0.8] {$d$} (s6);

    \end{tikzpicture}
        \caption*{\small (a) Un ejemplo de LTS y su particion correspondiente. Las transiciones no etiquetadas deben asumirse como intertes, es decir $\tau$-transitions.}
    \end{subfigure}
    \hfill
    % ==========================================
    % GRÁFICO (b) - Estructura de Datos
    % ==========================================
    \begin{subfigure}[b]{0.48\textwidth}
        \centering
        \begin{tikzpicture}[>=Stealth, node distance=0cm, scale=0.85, transform shape]

            % Nodos del Array
            \tikzstyle{cell}=[draw, minimum width=0.85cm, minimum height=0.6cm, anchor=center]
            
            \node[cell] (c6) {$s_6$};
            \node[cell, right=of c6] (c7) {$s_7$};
            \node[cell, right=of c7] (c8) {$s_8$};
            \node[cell, right=of c8] (c0) {$s_0$};
            \node[cell, right=of c0] (c5) {$s_5$};
            \node[cell, right=of c5] (c3) {$s_3$};
            \node[cell, right=of c3] (c1) {$s_1$};
            \node[cell, right=of c1] (c4) {$s_4$};
            \node[cell, right=of c4] (c2) {$s_2$};

            % Nodos Circulares Superiores
            \tikzstyle{bnode}=[draw, circle, inner sep=1pt, minimum size=0.6cm]
            
            \node[bnode] (b1) at ($(c6.north) + (0, 1cm)$) {$B_1$};
            \node[bnode] (b2) at ($(c7.north)!0.5!(c8.north) + (0, 1cm)$) {$B_2$};
            \node[bnode] (b3) at ($(c0.north) + (0, 1cm)$) {$B_3$};
            \node[bnode] (b4) at ($(c5.north)!0.5!(c3.north) + (0, 1cm)$) {$B_4$};
            \node[bnode] (b0) at ($(c1.north)!0.5!(c4.north) + (0, 1cm)$) {$B_0$};

            % Flechas hacia arriba
            \draw[->] (c6.north) -- (b1.south);
            \draw[->] (c7.north) -- (b2.south);
            \draw[->] (c8.north) -- (b2.south);
            \draw[->] (c0.north) -- (b3.south);
            \draw[->] (c5.north) -- (b4.south);
            \draw[->] (c3.north) -- (b4.south);
            \draw[->] (c1.north) -- (b0.south);
            \draw[->] (c4.north) -- (b0.south);
            \draw[->] (c2.north) -- (b0.south);

            % Llaves inferiores
            \draw[decorate,decoration={brace,amplitude=3pt,mirror}] (c6.south west) -- (c6.south east) node[midway,below=3pt] {\scriptsize bot.};
            \draw[decorate,decoration={brace,amplitude=3pt,mirror}] (c7.south west) -- (c8.south east) node[midway,below=3pt] {\scriptsize bot.};
            \draw[decorate,decoration={brace,amplitude=3pt,mirror}] (c0.south west) -- (c0.south east) node[midway,below=3pt] {\scriptsize bot.};
            \draw[decorate,decoration={brace,amplitude=3pt,mirror}] (c5.south west) -- (c3.south east) node[midway,below=3pt] {\scriptsize bot.};
            \draw[decorate,decoration={brace,amplitude=3pt,mirror}] (c1.south west) -- (c1.south east) node[midway,below=3pt] {\scriptsize bot.};

        \end{tikzpicture}
        \vspace{1.5cm} % Espaciado para que quede a la misma altura visual que el otro gráfico
        \caption*{\small (b) Estructura Refinable Partition para el LTS de ejemplo.}
    \end{subfigure}
    \caption{Una refinable partition para un LTS de ejemplo. (a) El LTS y su partición en cinco bloques; las transiciones sin etiqueta se asumen $\tau$ (inertes). (b) El array maestro \texttt{elements}: cada bloque es un slice contiguo, y dentro de cada slice los estados bottom (marcados \emph{bot.}) se ubican primero.}
    \label{fig:refinable-ejemplo}
\end{figure}

Las refinable partitions son una estructura de datos que reemplaza la forma de almacenar a $\Pi_s$ como una lista \texttt{List<Set<Long>>}, la estructura \texttt{stateToBlockMap} y el id de bloque \texttt{blockIdMap} de las primeras versiones. La idea principal es que todos los estados viven en un único array \texttt{elements}. Cada bloque es un slice contiguo \texttt{[start..end)} de ese array. Por lo tanto, al separar un bloque no se necesita copiar nuevamente la estructura, sino que se reordena el array con swaps $O(1)$ y se agrega un nuevo slice de bloque. La Figura~\ref{fig:refinable-ejemplo} (a) muestra un ejemplo de un LTS partido en 5 bloques, y la Figura~\ref{fig:refinable-ejemplo} (b) hace un snapshot de cómo se vería la estructura de Refinable Partition para el LTS de ejemplo, marcando también los slices contiguos de cada bloque. A su vez, dentro de cada bloque hay tres rangos disjuntos de estados, delimitados por dos punteros \texttt{bottomEnd} y \texttt{rDestEnd}. Esto se ve de forma poco clara pero introductoria en la Figura~\ref{fig:refinable-ejemplo} (b), donde se puede observar que los estados no-bottom de $B_0$ y $B_4$ están agrupados de forma separada dentro de cada bloque, luego de los estados bottom, marcados como ``bot.''. Una refinable partition tiene tres divisiones: primero en \texttt{[start..bottomEnd)}, se posicionan los estados bottom. Luego, en \texttt{[bottomEnd..rDestEnd)}, se almacenaran durante el algoritmo del split, los estados no-bottom que no van a estar en $R$. Por ultimo, posicionado al final de la estructura, en \texttt{[rDestEnd..end)}, se encontraran los estados destinados a $R$. Se mantiene siempre el invariante $\mathtt{start} \le \mathtt{bottomEnd} \le \mathtt{rDestEnd} \le \mathtt{end}$.

La implementacion de refinable partition se compone de dos clases: \texttt{RefinablePartition} y \texttt{Block}. La Figura~\ref{fig:class-block} detalla la clase Block, compuesta por el camppo de start, end, bottomEnd, rDestEnd, el id y si el bloque sigue activo, con alive. A su vez, expone tambien el metodo size. 

\begin{figure}[htbp]
  \centering
  \begin{minipage}{\linewidth}
  \begin{lstlisting}[language=Java, numbers=none]
    public static final class Block {
        int start;
        int end;
        int bottomEnd;
        int rDestEnd;
        public final int id;
        boolean alive;

        public int size()          { return end - start; }
    }
  \end{lstlisting}
  \end{minipage}
  \caption{Definicion de la clase Block}
  \label{fig:class-block}
\end{figure}

A su vez, se define la clase "RefinablePartition", detallada en la  Figura~\ref{fig:class-rp}. Se exponen los metodos de contruccion, \texttt{RefinablePartition(Collection<Long> universe)} y \texttt{void seedFromInitialBlocks(List<Set<Long>> initialBlocks)}. Luego, los metodos de lookup, donde se pueden notar claramente las mejoras en los tiempos de acceso en esta estructura. \texttt{Block blockOf(long s)} devuelve el bloque al que pertenece el estado s, en $O(1)$. \texttt{Iterable<Long> states(Block b)} retorna los estados del bloque b, en $O(|b|)$. \texttt{Iterable<Long> bottoms(Block b)} retorna los estados bottoms del bloque b, en $O(|cant estados bottom de b|)$. \texttt{Iterable<Long> rDestStates(Block b)} retorna todos los estados destinados a R del bloque B, utilizando el puntero ya mencionado, $rDestEnd$, en $O(|b.rDestCount|)$. Por ultimo, \texttt{List<Block> liveBlocks()} retorna todos los bloques existentes en la RefinablePartition. Luego, se exponen tambien los metodos necesarios para realizar un split.

\begin{figure}[htbp]
  \centering
  \begin{minipage}{\linewidth}
  \begin{lstlisting}[language=Java, numbers=none]
    final class RefinablePartition {
      RefinablePartition(Collection<Long> universe);
      void seedFromInitialBlocks(List<Set<Long>> initialBlocks);

      Block blockOf(long s);                  
      Iterable<Long> states(Block b);         
      Iterable<Long> bottoms(Block b);        
      Iterable<Long> rDestStates(Block b);    
      List<Block> liveBlocks();              

      void addToR(long s);                    
      void clearR(Block b);                  
      Pair<Block, Block> splitOffR(Block b);  

      void markAsBottom(long s);
      void resetBottoms(Block b);
    }
  \end{lstlisting}
  \end{minipage}
  \caption{Definicion de la clase RefinablePartition}
  \label{fig:class-rp}
\end{figure}

La Figura~\ref{fig:refinable-split} sigue cómo va mutando la estructura durante un split, paso a paso. Observar este proceso ayudara a explicar como funcionan los metodos \texttt{addToR}, \texttt{clearR} y \texttt{splitOffR} que son el corazon de la \texttt{RefinablePartition}. Observemos el slice de la estructura \texttt{RefinablePartition} que representa al bloque B. Para ejecutar las operaciones, lo único que se mueve son los estados dentro del slice de $B$ (a traves de swaps) y los punteros \texttt{bottomEnd} y \texttt{rDestEnd}. En ningún momento se copian estados a una estructura nueva ni a otro slice. A continuacion, el paso a paso de un split.

\colorlet{cBot}{gray!25}
\colorlet{cU}{white}
\colorlet{cR}{red!18}
\newcommand{\rpBox}[5]{%
    \draw[fill=#5] (#1, #3 - 0.35) rectangle (#2, #3 + 0.35);%
    \node at ({(#1+#2)/2}, #3) {#4};}
\newcommand{\rpPtr}[4]{%
    \node[font=\ttfamily\scriptsize, text=#4, anchor=south] at (#1, #2 + 0.78) {#3};%
    \draw[#4, thick] (#1, #2 + 0.75) -- (#1, #2 + 0.4);}

\begin{figure}[h]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[font=\sffamily]
    \rpPtr{0}{0}{start}{black}
    \rpPtr{3}{0}{bottomEnd}{blue!60!black}
    \rpPtr{12}{0}{rDestEnd = end}{red!70!black}
    \draw[decorate, decoration={brace, amplitude=5pt}]
        (0, 1.45) -- (12, 1.45) node[midway, above=4pt] {Bloque $B$ que se va a partir};
    \rpBox{0}{3}{0}{bottom}{cBot}
    \rpBox{3}{12}{0}{no-bottom}{cU}
\end{tikzpicture}%
}
\caption{Paso (a): estado inicial del bloque $B$ (\texttt{clearR}).}
\label{fig:refinable-split}
\end{figure}

\textbf{(a) \textbf{\texttt{clearR(B)}}} (Figura~\ref{fig:refinable-split}). Por default, un bloque comienza con \texttt{rDestEnd = end}, por lo que la particion destinada a $R$ queda vacío. El bloque contiene sólo dos rangos visibles: los estados bottom \texttt{[start..bottomEnd)} y el resto no-bottom. La función \texttt{clearR} asegura que la particion de $R$ comience vacía.

\begin{figure}[h]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[font=\sffamily]
    \rpBox{0}{3}{0}{bottom}{cBot}
    \rpBox{3}{11}{0}{no-bottom}{cU}
    \rpBox{11}{12}{0}{$s$}{cR}
    \rpPtr{11}{0}{rDestEnd}{red!70!black}
\end{tikzpicture}%
}
\caption[]{Paso (b): \texttt{addToR} sobre un estado no-bottom.}
\label{fig:refinable-split-b}
\end{figure}

\textbf{(b) \textbf{\texttt{addToR(s)}} sobre un no-bottom} (Figura~\ref{fig:refinable-split-b}). Si se debe agregar un estado $s$ no-bottom a $R$, se hace un swap del estado contra la posición \texttt{rDestEnd-1} y se retrocede \texttt{rDestEnd} en uno. La particion $R$ crece de a un estado por llamada, sin tocar los estados bottom. $O(1)$ ya que solo se debe realizar el swap y sumar o restar a los punteros.

\begin{figure}[h]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[font=\sffamily]
    \rpBox{0}{2}{0}{bottom}{cBot}
    \rpBox{2}{10}{0}{no-bottom}{cU}
    \rpBox{10}{12}{0}{$R$}{cR}
    \rpPtr{2}{0}{bottomEnd}{blue!60!black}
    \rpPtr{10}{0}{rDestEnd}{red!70!black}
\end{tikzpicture}%
}
\caption[]{Paso (c): \texttt{addToR} sobre un estado bottom.}
\label{fig:refinable-split-c}
\end{figure}

\textbf{(c) \textbf{\texttt{addToR(b)}} sobre un bottom} (Figura~\ref{fig:refinable-split-c}). Si el estado era bottom, primero se lo saca de ahí con un swap contra \texttt{bottomEnd-1} y se retrocede \texttt{bottomEnd}. D esta forma, el estado queda incluido en la particion $R$ al igual que en (b). $O(1)$ ya que solo se debe realizar el swap y sumar o restar a los punteros.

\begin{figure}[h]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[font=\sffamily]
    \rpBox{0}{2}{0}{bottom}{cBot}
    \rpBox{2}{7}{0}{no-bottom}{cU}
    \rpBox{7}{12}{0}{$R$}{cR}
    \rpPtr{0}{0}{start}{black}
    \rpPtr{2}{0}{bottomEnd}{blue!60!black}
    \rpPtr{7}{0}{rDestEnd}{red!70!black}
    \rpPtr{12}{0}{end}{red!70!black}
\end{tikzpicture}%
}
\caption[]{Paso (d): estado del bloque tras repetir \texttt{addToR}.}
\label{fig:refinable-split-d}
\end{figure}

\textbf{(d) Estado tras repetir \textbf{\texttt{addToR}}} (Figura~\ref{fig:refinable-split-d}). Una vez agregados todos los estados de $R$, la particion \texttt{[rDestEnd..end)} contiene los estados indicados y el slice de $B$ quedó ordenado en tres particiones.

\begin{figure}[h]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[font=\sffamily]
    \rpBox{0}{2}{0}{bottom}{cBot}
    \rpBox{2}{7}{0}{no-bottom}{cU}
    \rpBox{7.3}{12}{0}{$R$}{cR}
    \draw[decorate, decoration={brace, amplitude=4pt, mirror}]
        (0, -0.45) -- (7, -0.45) node[midway, below=4pt] {\small \texttt{uBlock}};
    \draw[decorate, decoration={brace, amplitude=4pt, mirror}]
        (7.3, -0.45) -- (12, -0.45) node[midway, below=4pt] {\small \texttt{rBlock}};
\end{tikzpicture}%
}
\caption[]{Paso (e): \texttt{splitOffR} divide el bloque en \texttt{uBlock} y \texttt{rBlock}.}
\label{fig:refinable-split-e}
\end{figure}

\textbf{(e) \textbf{\texttt{splitOffR(B)}}} (Figura~\ref{fig:refinable-split-e}). Se corta el slice en \texttt{rDestEnd}: la primera particion \texttt{[start..rDestEnd)} pasa a ser \texttt{uBlock} y la segunda particion \texttt{[rDestEnd..end)} pasa a ser \texttt{rBlock}, cada uno con su nuevo id. El viejo $B$ se marca como muerto y se reasigna \texttt{blockOfElement} únicamente en las posiciones del sufijo para que apunten a \texttt{rBlock}. Es la única pasada lineal del split y reemplaza a la copia de dos \texttt{HashSet} de las versiones anteriores. $O(1)$ ya que lo unico que se hace es separar a R como otro bloque. Esto implica crear una nueva estructura Block con los punteros de start y end, pero no implica copiar todos los elementos de R. 

\begin{figure}[h]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[font=\sffamily]
    \rpBox{0}{1.5}{0}{bottom}{cBot}
    \rpBox{1.5}{7}{0}{no-bottom}{cU}
    \rpBox{7.3}{9}{0}{bottom}{cBot}
    \rpBox{9}{12}{0}{no-bottom}{cU}
\end{tikzpicture}%
}
\caption[]{Paso (f): \texttt{findBottomStates} recomputa los estados bottom en cada bloque nuevo.}
\label{fig:refinable-split-f}
\end{figure}

\textbf{(f) \textbf{\texttt{findBottomStates}}} (Figura~\ref{fig:refinable-split-f}). Como al hacer el split los bottoms del bloque viejo ya no valen,se recomputan utilizando \texttt{resetBottoms}, que realiza \texttt{bottomEnd = start} y \texttt{markAsBottom} que realiza un swap y mueve el puntero \texttt{bottomEnd} segun corresponda.

Para concluir, tambien se reemplazo la inicializacion de $\Pi_s$, que en las anteriores versiones era implementado como una lista \texttt{List<Set<Long>>}, por las llamadas a los contructores de RefinablePartitions, tal como se ve en la Figura~\ref{fig:init-pi-s}. Asi, se logra bajar la complejidad de varias operaciones frecuentes de $\Pi_s$. Por ejemplo, cuando previamente se queria ver si $\Pi_s$ contenia un bloque B, se debia recorrer toda la lista de bloques, es decir que tenia complejidad $O(|\Pi_s|)$. Con la nueva estructura, esto se realiza en O(1), simplemente viendo si el bloque B tiene el campo \texttt{alive} en \texttt{true}. A su vez, para encontrar los bottom states de un bloque B, se iteraba por todos los estados hasta encontrar los bottom. Ahora, se pueden encontrar todos los bottoms directamente en la particion \texttt{[start..bottomEnd)}. 

\begin{figure}[htbp]
  \centering
  \begin{minipage}{\linewidth}
  \begin{lstlisting}[language=Java, numbers=none]
    RefinablePartition Pi_s = new RefinablePartition(toMinimise.getStates());
    Pi_s.seedFromInitialBlocks(initialBlocks);
  \end{lstlisting}
  \end{minipage}
  \caption{Inicializacion de $\Pi_s$ como RefinablePartition}
  \label{fig:init-pi-s}
\end{figure}


\subsection{Linked Transition Partitions}
En el paper de Jansen tambien se sugiere la implementacion de $\Pi_t$ como "linked refinable partitions". Asi, la idea es reemplazar los bunches de  $\Pi_t$  que en las primeras versiones estaban representados como \texttt{Set<Triple<Long, String, Long>>}. Para ello, se define la clase \texttt{Transition} tal como muestra la Figura~\ref{fig:class-transition}, donde cada transicion es un objeto que almacena el estado de origen, la accion y el estado destino. A su vez, se almacena el bunch de $\Pi_t$ que la contiene, y el block-bunch al que pertence, es decir el subgrupo que indica dentro de la bunch a la que pertence, de que estado de origen es. Por ultimo, se implementa el campo untested, necesario para la implementacion de las corrutinas en el split. 
\begin{figure}[htbp]
  \centering
  \begin{minipage}{\linewidth}
  \begin{lstlisting}[language=Java, numbers=none]
    public static final class Transition {
        public final long source;
        public final String action;
        public final long target;
        BunchSlice bunch;
        BlockBunchSlice blockBunch;
        int untested;
    }
  \end{lstlisting}
  \end{minipage}
  \caption{Definicion de la clase Transition}
  \label{fig:class-transition}
\end{figure}

Luego, se define la clase \texttt{LinkedTransitionPartitions}, tal como se ve en la Figura~\ref{fig:class-ltp}. Se hace referencia a $\Pi_s$ ya que se debe consultar en diversos metodos, debido a que los bunches dependen de los bloques de estados.  La cola de trabajo El campo \texttt{globalUnstable} es la lista de todas las block-bunch-slices inestables, es decir, la cola de trabajo pendiente de la Fase 2: implementada como una lista con \emph{position tracking}, permite agregar y quitar elementos en $O(1)$. Por ultimo, \texttt{nextBunchId} y \texttt{nextBlockBunchId} son contadores que asignan un identificador unico a cada \texttt{BunchSlice} y \texttt{BlockBunchSlice} en el momento de su creacion. A su vez, esta la lista \texttt{allTransitions}, que es la lista que contiene todas las transiciones del LTS. Esta es la lista principal sobre la cual se construyen las cuatro vistas de las transiciones descritas en el paper de Jansen que permiten sostener la complejidad deseada.

\begin{figure}[htbp]
  \centering
  \begin{minipage}{\linewidth}
  \begin{lstlisting}[language=Java, numbers=none]
    public final class LinkedTransitionPartitions {
      RefinablePartition Pi_s;
      List<Transition> allTransitions;
      Map<Long, List<Transition>> bySource;
      Map<Long, List<Transition>> byTarget;
      List<BunchSlice> liveBunches;
      Map<RefinablePartition.Block, List<BlockBunchSlice>> slicesByBlock;
      List<BlockBunchSlice> globalUnstable;
      Map<Triple<Long, String, Long>, Transition> transitionLookup;

      nextBunchId;
      nextBlockBunchId;
    }
  \end{lstlisting}
  \end{minipage}
  \caption{Definicion de la clase LinkedTransitionPartitions}
  \label{fig:class-ltp}
\end{figure}

Por un lado, esta la vista por bunch \texttt{liveBunches}. Esta vista se implementa como una lista de bunch slices \texttt{List<BunchSlice>}, donde la clase \texttt{BunchSlice} agrupa las transiciones de un mismo bunch de $\Pi_t$. Esta es la vista que implementa directamente la particion $\Pi_t$, y reemplaza al \texttt{IdentityHashMap} que en las versiones anteriores representaba el conjunto de bunches. La Figura~\ref{fig:vista-a} la muestra como queda la estructura sobre el ejemplo de la Figura~\ref{fig:refinable-ejemplo}. A diferencia del paper, esta vista no mantiene de forma explicita los action-block-slices (los subconjuntos de transiciones de un mismo bunch con igual accion y bloque destino), sino que estos se calculan momentaneamente solo cuando hace falta refinar un bunch, en el metodo \texttt{peelSlice}.

\begin{figure}[h]
  \centering
  \begin{tikzpicture}[
    >=stealth,
    sbox/.style={draw, rectangle, minimum width=0.95cm, minimum height=0.55cm, inner sep=0pt, outer sep=0pt, font=\scriptsize},
    ell/.style={draw, ellipse, inner sep=1.5pt, font=\scriptsize},
    circ/.style={draw, circle, inner sep=1.5pt, font=\scriptsize},
    node distance=-\pgflinewidth
  ]

    % Leaf nodes (boxes)
    \node[sbox] (a0) {$s_3 \xrightarrow{a} s_7$};
    \node[sbox, right=of a0] (a1) {$s_1 \xrightarrow{a} s_7$};
    \node[sbox, right=of a1] (a2) {$s_5 \xrightarrow{a} s_7$};
    
    \node[sbox, right=of a2] (a3) {$s_0 \xrightarrow{c} s_8$};
    
    \node[sbox, right=of a3] (a4) {$s_1 \xrightarrow{b} s_8$};
    \node[sbox, right=of a4] (a5) {$s_7 \xrightarrow{b} s_7$};
    \node[sbox, right=of a5] (a6) {$s_8 \xrightarrow{b} s_8$};
    
    \node[sbox, right=of a6] (a7) {$s_5 \xrightarrow{d} s_6$};
    \node[sbox, right=of a7] (a8) {$s_2 \xrightarrow{d} s_6$};
    \node[sbox, right=of a8] (a9) {$s_0 \xrightarrow{d} s_6$};
    
    \node[sbox, right=of a9] (a10) {$s_4 \xrightarrow{\tau} s_5$};
    \node[sbox, right=of a10] (a11) {$s_4 \xrightarrow{\tau} s_3$};
    \node[sbox, right=of a11] (a12) {$s_2 \xrightarrow{\tau} s_3$};
    \node[sbox, right=of a12] (a13) {$s_1 \xrightarrow{\tau} s_3$};

    % Root nodes (bunches)
    \node[circ] (R1) at ($(a0)!0.5!(a2) + (0, 1.5)$) {$T''$};
    \node[circ] (R2) at ($(a3)!0.5!(a6) + (0, 1.5)$) {$T$};
    \node[circ] (R3) at ($(a7)!0.5!(a9) + (0, 1.5)$) {$T'$};
    \node[circ] (R4) at ($(a10)!0.5!(a13) + (0, 1.5)$) {$T_\tau$};

    % Arrows
    \foreach \i in {0,1,2} \draw[->] (a\i.north) -- (R1);
    \foreach \i in {3,4,5,6} \draw[->] (a\i.north) -- (R2);
    \foreach \i in {7,8,9} \draw[->] (a\i.north) -- (R3);
    \foreach \i in {10,11,12,13} \draw[->] (a\i.north) -- (R4);

  \end{tikzpicture}
  \caption{La estrctura de \texttt{liveBunches}, que representa la vista de todas las transiciones ordenadas por bunches de $\Pi_t$. Se usa el ejemplo de la Figura~\ref{fig:refinable-ejemplo}. }
  \label{fig:vista-a}
\end{figure}

Luego, la segunda vista es \texttt{slicesByBlock}, que se implementa como un mapa \texttt{Map<Block, List<BlockBunchSlice>>}, donde cada bloque de $\Pi_s$ tiene asociada una lista de todos sus block-bunch-slices, que serian las transiciones que se originan desde bloque dentro de cada bunch. Este mapa permite recuperar en $O(1)$ todas las block-bunch-slices de un bloque, lo cual es utilizado cuando se parte un bloque, ya que se debe redistribuir las block-bunch-slices del bloque viejo entre los dos bloques nuevos. La Figura~\ref{fig:vista-b} muestra la estructura de  \texttt{slicesByBlock} para el ejemplo presentado anteriormente.




\begin{figure}[htbp]
  \centering
  \resizebox{\textwidth}{!}{%
  \begin{tikzpicture}[
    >=stealth,
    sbox/.style={draw, rectangle, minimum width=0.95cm, minimum height=0.55cm, inner sep=0pt, outer sep=0pt, font=\scriptsize},
    ell/.style={draw, ellipse, inner sep=1.5pt, font=\scriptsize},
    circ/.style={draw, circle, inner sep=1.5pt, font=\scriptsize},
    node distance=-\pgflinewidth
  ]

    % Leaf nodes (boxes), grouped by source block
    \node[sbox] (b0) {$s_1 \xrightarrow{a} s_7$};
    \node[sbox, right=of b0] (b1) {$s_0 \xrightarrow{c} s_8$};
    \node[sbox, right=of b1] (b2) {$s_1 \xrightarrow{b} s_8$};
    \node[sbox, right=of b2] (b3) {$s_2 \xrightarrow{d} s_6$};
    \node[sbox, right=of b3] (b4) {$s_0 \xrightarrow{d} s_6$};
    \node[sbox, right=of b4] (b5) {$s_4 \xrightarrow{\tau} s_5$};
    \node[sbox, right=of b5] (b6) {$s_4 \xrightarrow{\tau} s_3$};
    \node[sbox, right=of b6] (b7) {$s_2 \xrightarrow{\tau} s_3$};
    \node[sbox, right=of b7] (b8) {$s_1 \xrightarrow{\tau} s_3$};
    \node[sbox, right=of b8] (b9) {$s_7 \xrightarrow{b} s_7$};
    \node[sbox, right=of b9] (b10) {$s_8 \xrightarrow{b} s_8$};
    \node[sbox, right=of b10] (b11) {$s_3 \xrightarrow{a} s_7$};
    \node[sbox, right=of b11] (b12) {$s_5 \xrightarrow{a} s_7$};
    \node[sbox, right=of b12] (b13) {$s_5 \xrightarrow{d} s_6$};

    % Intermediate nodes (block-bunch-slices), notation T_B ->
    \node[ell] (M1) at ($(b0) + (0, 1.5)$) {$T''_{B_0}{\rightarrow}$};
    \node[ell] (M2) at ($(b1)!0.5!(b2) + (0, 1.5)$) {$T_{B_0}{\rightarrow}$};
    \node[ell] (M3) at ($(b3)!0.5!(b4) + (0, 1.5)$) {$T'_{B_0}{\rightarrow}$};
    \node[ell] (M4) at ($(b5)!0.5!(b8) + (0, 1.5)$) {$(T_\tau)_{B_0}{\rightarrow}$};
    \node[ell] (M5) at ($(b9)!0.5!(b10) + (0, 1.5)$) {$T_{B_2}{\rightarrow}$};
    \node[ell] (M6) at ($(b11)!0.5!(b12) + (0, 1.5)$) {$T''_{B_4}{\rightarrow}$};
    \node[ell] (M7) at ($(b13) + (0, 1.5)$) {$T'_{B_4}{\rightarrow}$};

    % Root nodes (source blocks)
    \node[circ] (P0) at ($(M1)!0.5!(M4) + (0, 1.5)$) {$B_0$};
    \node[circ] (P2) at ($(M5) + (0, 1.5)$) {$B_2$};
    \node[circ] (P4) at ($(M6)!0.5!(M7) + (0, 1.5)$) {$B_4$};

    % Arrows
    \draw[->] (b0.north) -- (M1);
    \foreach \i in {1,2} \draw[->] (b\i.north) -- (M2);
    \foreach \i in {3,4} \draw[->] (b\i.north) -- (M3);
    \foreach \i in {5,6,7,8} \draw[->] (b\i.north) -- (M4);
    \foreach \i in {9,10} \draw[->] (b\i.north) -- (M5);
    \foreach \i in {11,12} \draw[->] (b\i.north) -- (M6);
    \draw[->] (b13.north) -- (M7);

    \draw[->] (M1.north) -- (P0);
    \draw[->] (M2.north) -- (P0);
    \draw[->] (M3.north) -- (P0);
    \draw[->] (M4.north) -- (P0);
    \draw[->] (M5.north) -- (P2);
    \draw[->] (M6.north) -- (P4);
    \draw[->] (M7.north) -- (P4);

  \end{tikzpicture}%
  }
  \caption{La estructura de \texttt{slicesByBlock}, mostrando la vista de las transiciones agrupadas por block-bunch-slice.}
  \label{fig:vista-b}
\end{figure}

La tercera vista mencionada agrupa las transiciones por su estado de origen. Se implementa como un mapa de tipo \texttt{Map<Long, List<Transition>>} llamado \texttt{bySource} y se expone con el metodo \texttt{outgoing(s)}, que devuelve todas las transiciones salientes del estado s. Esta vista es \emph{estatica}, ya que como la lista de transiciones salientes de un estado se fija segun el LTS de entrada y no varia, la estructura no se modifica en ningun paso del algoritmo. Se usa de referencia para poder obtener todas las transiciones salientes de un estado, por ejemplo, durante el split. La Figura~\ref{fig:vista-c} muestra la estructura, donde cada estado de origen tiene asociada la lista de sus transiciones salientes.
% A diferencia del paper, en esta implementacion las transiciones inertes no se almacenan en \texttt{LinkedTransitionPartitions}, ya que la alcanzabilidad inerte se resuelve con la descomposicion en componentes fuertemente conexas por $\tau$ precomputada al inicio. Por eso, \texttt{bySource} solo contiene transiciones no-inertes.

\begin{figure}[htbp]
  \centering
  \resizebox{\textwidth}{!}{%
  \begin{tikzpicture}[
    >=stealth,
    sbox/.style={draw, rectangle, minimum width=0.95cm, minimum height=0.55cm, inner sep=0pt, outer sep=0pt, font=\scriptsize},
    ell/.style={draw, ellipse, inner sep=1.5pt, font=\scriptsize},
    node distance=-\pgflinewidth
  ]

    % Leaf nodes, grouped by source state
    \node[sbox] (c0) {$s_0 \xrightarrow{c} s_8$};
    \node[sbox, right=of c0] (c1) {$s_0 \xrightarrow{d} s_6$};
    \node[sbox, right=of c1] (c2) {$s_1 \xrightarrow{a} s_7$};
    \node[sbox, right=of c2] (c3) {$s_1 \xrightarrow{b} s_8$};
    \node[sbox, right=of c3] (c4) {$s_1 \xrightarrow{\tau} s_3$};
    \node[sbox, right=of c4] (c5) {$s_2 \xrightarrow{d} s_6$};
    \node[sbox, right=of c5] (c6) {$s_2 \xrightarrow{\tau} s_3$};
    \node[sbox, right=of c6] (c7) {$s_3 \xrightarrow{a} s_7$};
    \node[sbox, right=of c7] (c8) {$s_4 \xrightarrow{\tau} s_5$};
    \node[sbox, right=of c8] (c9) {$s_4 \xrightarrow{\tau} s_3$};
    \node[sbox, right=of c9] (c10) {$s_5 \xrightarrow{a} s_7$};
    \node[sbox, right=of c10] (c11) {$s_5 \xrightarrow{d} s_6$};
    \node[sbox, right=of c11] (c12) {$s_7 \xrightarrow{b} s_7$};
    \node[sbox, right=of c12] (c13) {$s_8 \xrightarrow{b} s_8$};

    % Root nodes (source states)
    \node[ell] (S0) at ($(c0)!0.5!(c1) + (0, 1.5)$) {out($s_0$)};
    \node[ell] (S1) at ($(c2)!0.5!(c4) + (0, 1.5)$) {out($s_1$)};
    \node[ell] (S2) at ($(c5)!0.5!(c6) + (0, 1.5)$) {out($s_2$)};
    \node[ell] (S3) at ($(c7) + (0, 1.5)$) {out($s_3$)};
    \node[ell] (S4) at ($(c8)!0.5!(c9) + (0, 1.5)$) {out($s_4$)};
    \node[ell] (S5) at ($(c10)!0.5!(c11) + (0, 1.5)$) {out($s_5$)};
    \node[ell] (S7) at ($(c12) + (0, 1.5)$) {out($s_7$)};
    \node[ell] (S8) at ($(c13) + (0, 1.5)$) {out($s_8$)};

    % Arrows
    \foreach \i in {0,1} \draw[->] (c\i.north) -- (S0);
    \foreach \i in {2,3,4} \draw[->] (c\i.north) -- (S1);
    \foreach \i in {5,6} \draw[->] (c\i.north) -- (S2);
    \draw[->] (c7.north) -- (S3);
    \foreach \i in {8,9} \draw[->] (c\i.north) -- (S4);
    \foreach \i in {10,11} \draw[->] (c\i.north) -- (S5);
    \draw[->] (c12.north) -- (S7);
    \draw[->] (c13.north) -- (S8);

  \end{tikzpicture}%
  }
  \caption{La estructura(\texttt{bySource}), que representa la vista de transiciones ordenadas por estado de origen.}
  \label{fig:vista-c}
\end{figure}

Por ultimo, la cuarta vista es la otra de la anterior y agrupa las transiciones por su estado destino, tal como muestra la Figura~\ref{fig:vista-d}. Se implementa como un mapa de transiciones \texttt{Map<Long, List<Transition>>} denotado \texttt{byTarget} y se expone con el metodo \texttt{incoming(s)}, que devuelve las traniciones entrantes al estado s. Por el mismo motivo que la vista anterior, tambien es estatica. Optimiza de forma el proceso de cuando un bloque se parte, ya que se deben marcar como inestables las block-bunch-slices de los bunches que tenian transiciones apuntando a los estados del bloque que se separo, y \texttt{byTarget} permite acceder a ellas facilmente recorriendo para cada estado del nuevo bloque, sus transiciones entrantes.

\begin{figure}[htbp]
  \centering
  \resizebox{\textwidth}{!}{%
  \begin{tikzpicture}[
    >=stealth,
    sbox/.style={draw, rectangle, minimum width=0.95cm, minimum height=0.55cm, inner sep=0pt, outer sep=0pt, font=\scriptsize},
    ell/.style={draw, ellipse, inner sep=1.5pt, font=\scriptsize},
    node distance=-\pgflinewidth
  ]

    % Leaf nodes, grouped by target state
    \node[sbox] (d0) {$s_4 \xrightarrow{\tau} s_3$};
    \node[sbox, right=of d0] (d1) {$s_2 \xrightarrow{\tau} s_3$};
    \node[sbox, right=of d1] (d2) {$s_1 \xrightarrow{\tau} s_3$};
    \node[sbox, right=of d2] (d3) {$s_4 \xrightarrow{\tau} s_5$};
    \node[sbox, right=of d3] (d4) {$s_5 \xrightarrow{d} s_6$};
    \node[sbox, right=of d4] (d5) {$s_2 \xrightarrow{d} s_6$};
    \node[sbox, right=of d5] (d6) {$s_0 \xrightarrow{d} s_6$};
    \node[sbox, right=of d6] (d7) {$s_3 \xrightarrow{a} s_7$};
    \node[sbox, right=of d7] (d8) {$s_1 \xrightarrow{a} s_7$};
    \node[sbox, right=of d8] (d9) {$s_5 \xrightarrow{a} s_7$};
    \node[sbox, right=of d9] (d10) {$s_7 \xrightarrow{b} s_7$};
    \node[sbox, right=of d10] (d11) {$s_0 \xrightarrow{c} s_8$};
    \node[sbox, right=of d11] (d12) {$s_1 \xrightarrow{b} s_8$};
    \node[sbox, right=of d12] (d13) {$s_8 \xrightarrow{b} s_8$};

    % Root nodes (target states)
    \node[ell] (I3) at ($(d0)!0.5!(d2) + (0, 1.5)$) {in($s_3$)};
    \node[ell] (I5) at ($(d3) + (0, 1.5)$) {in($s_5$)};
    \node[ell] (I6) at ($(d4)!0.5!(d6) + (0, 1.5)$) {in($s_6$)};
    \node[ell] (I7) at ($(d7)!0.5!(d10) + (0, 1.5)$) {in($s_7$)};
    \node[ell] (I8) at ($(d11)!0.5!(d13) + (0, 1.5)$) {in($s_8$)};

    % Arrows
    \foreach \i in {0,1,2} \draw[->] (d\i.north) -- (I3);
    \draw[->] (d3.north) -- (I5);
    \foreach \i in {4,5,6} \draw[->] (d\i.north) -- (I6);
    \foreach \i in {7,8,9,10} \draw[->] (d\i.north) -- (I7);
    \foreach \i in {11,12,13} \draw[->] (d\i.north) -- (I8);

  \end{tikzpicture}%
  }
  \caption{La estructura  \texttt{byTarget}, mostrando las transiciones agrupadas por estado destino.}
  \label{fig:vista-d}
\end{figure}

Es importante aclarar que las cuatro vistas no implica que haya cuatro copias de las transiciones, sino que existen cuatro agrupaciones de los mismos objetos \texttt{Transition}, ya que cada transicion guarda punteros a su \texttt{BunchSlice} y a su \texttt{BlockBunchSlice}, y el puntero a la propia \texttt{Transition} es lo que se relaciona en las listas de \texttt{bySource} y \texttt{byTarget}. Es por esto que el paper las llama "linked", ya que una unica instancia participa de las cuatro vistas/particiones a la vez.

\subsection{El método split como corrutinas}
Como se explicó anteriormente, la función split en el paper de Jansen se idea originalmente como dos corrutinas, donde la corrutina de R comienza con los estados iniciales que deben ir en R y luego extiende el conjunto hacia atrás, recorriendo las transiciones inertes. A su vez, la corrutina de U identifica a los estados que no están en R, contando por cada estado cuántos son sus sucesores que todavía no están en U. Ejecutar estas corrutinas de forma alternada, un paso cada una (en lockstep), es la esencia de la complejidad del paper, ya que en cuanto una corrutina agrega a su conjunto más de la mitad de estados totales que tenía el bloque, se corta su ejecución. Este corte consigue que cada llamada a la función split pase de tener que recorrer todo el bloque en peor caso, a que pase a recorrer solo el subbloque más chico de los dos conjuntos de estados resultantes. Sin embargo, en las primeras versiones la implementación se había reducido a realizar backwards-reachability para R, es decir, no se había desarrollado la optimización sugerida en el paper. Por su importancia en la complejidad final, era esencial implementarla en nuestra versión en MTSA. Sin embargo, Java no tiene la posibilidad de implementar corrutinas de forma nativa.

Para poder subsanar esa dificultad en la implementación, se implementaron dos funciones: rStep que ejecuta un paso en el análisis del bloque R y uStep que ejecuta un paso en el análisis del bloque U. Ahora, ¿cómo representamos qué es un ''paso'' en las funciones? Para esta implementación, un paso en cada función es analizar el próximo estado que corresponde chequear si pertenece al subbloque de la función. Para ello, se implementaron dos estructuras de colas, una para cada función, que contienen los estados que faltan analizar. En cada paso, cada corrutina toma el siguiente paso y ejecuta el análisis correspondiente para ver si ese estado corresponde o no en su conjunto. A su vez, se implementan los contadores ''rSize'' y ''uSize''. Esto permite validar que cuando alguno de los dos contadores supere la mitad de los estados del bloque, se deje de ejecutar esa corrutina y no se sigan analizando estados para ese subbloque, haciendo que solo finalice la corrutina del subbloque más chico y luego se calcule el conjunto final de estados para cada subbloque, tal como se detalla en el paper y tal como fue explicado en la sección previa.

Si bien esta implementacion no mantiene de manera completamente fiel el detalle del paper, permite que repliquemos la cota de la complejidad dentro de las limitaciones del lenguaje utilizado. 

